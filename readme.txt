First of all your Laptop and Mobile on same network

To run this app instructions:

Open Terminal (cmd)
run these commad:
cd server
then,
npm run dev

if Mongodbsuccessfully run Display 
then open new Terminal and run

npx expo start --clear

then open service/api.ts and match your Url
example:
if in terminal it display URL just below QRCode  (192.168.1.5)
then your api.ts file URL should also bhi same (192.168.1.5)

